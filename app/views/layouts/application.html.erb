<!DOCTYPE html>
<html lang="en">
<head>
  <title>ProjectConversation</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="bg-gradient-to-r from-blue-50 to-blue-100 min-h-screen flex flex-col">
<!-- Navbar -->
<nav class="bg-gradient-to-r from-blue-600 to-blue-800 p-6 shadow-lg sticky top-0 z-50">
  <div class="container mx-auto flex justify-between items-center">
    <h1 class="text-3xl font-bold text-white"><%= link_to 'ProjectConversation', root_path %></h1>

    <div class="flex items-center space-x-4">
      <% if user_signed_in? %>
        <%= link_to 'Projects', projects_path, class: 'text-white hover:bg-blue-700 px-4 py-2 rounded-md transition duration-300 ease-in-out' %>
        <%= link_to 'Edit Profile', edit_user_registration_path, class: 'text-white hover:bg-blue-700 px-4 py-2 rounded-md transition duration-300 ease-in-out' %>
        <p class="text-white">Welcome, <%= current_user.email %>!</p>
        <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'text-white bg-red-500 hover:bg-red-600 px-4 py-2 rounded-md transition duration-300 ease-in-out' %>
      <% else %>
        <%= link_to 'Login', new_user_session_path, class: 'text-white hover:bg-blue-700 px-4 py-2 rounded-md transition duration-300 ease-in-out' %>
        <%= link_to 'Sign Up', new_user_registration_path, class: 'text-white bg-green-500 hover:bg-green-600 px-4 py-2 rounded-md transition duration-300 ease-in-out' %>
      <% end %>
    </div>
  </div>
</nav>

<!-- Main Content Area -->
<main class="container mx-auto p-6 flex-grow">
  <% if notice %>
    <div class="bg-green-50 border-l-4 border-green-500 text-green-700 p-4 rounded-lg mb-6 shadow-md flex items-center">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
      </svg>
      <p class="ml-2"><%= notice %></p>
    </div>
  <% end %>
  <% if alert %>
    <div class="bg-red-50 border-l-4 border-red-500 text-red-700 p-4 rounded-lg mb-6 shadow-md flex items-center">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
      <p class="ml-2"><%= alert %></p>
    </div>
  <% end %>

  <%= yield %>
</main>

<!-- Footer -->
<footer class="bg-gray-200 py-6 text-center text-gray-600 mt-auto">
  &copy; <%= Time.current.year %> ProjectConversation. All rights reserved.
</footer>
</body>
</html>
