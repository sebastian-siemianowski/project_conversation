<% if errors.any? %>
  <div class="error-messages">
    <h2><%= pluralize(errors.count, "error") %> prohibited this project from being saved:</h2>
    <ul>
      <% errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
