<!DOCTYPE html>
<html lang="en">
<head>
  <title>ProjectConversation</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="bg-gray-100 min-h-screen flex flex-col">
<!-- Navbar -->
<nav class="bg-white shadow-md sticky top-0 z-50 p-4">
  <div class="container mx-auto flex justify-between items-center">
    <h1 class="text-2xl font-bold text-gray-900"><%= link_to 'ProjectConversation', root_path, class: 'hover:text-blue-500 transition duration-300' %></h1>

    <div class="flex items-center space-x-6">
      <% if user_signed_in? %>
        <%= link_to 'Projects', projects_path, class: 'text-gray-700 hover:text-blue-500 px-3 py-2 rounded-md transition duration-300' %>
        <%= link_to 'Edit Profile', edit_user_registration_path, class: 'text-gray-700 hover:text-blue-500 px-3 py-2 rounded-md transition duration-300' %>
        <p class="text-gray-700">Welcome, <%= current_user.email %>!</p>
        <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'text-white bg-gradient-to-br from-pink-500 to-orange-400 hover:from-pink-600 hover:to-orange-500 focus:ring-4 focus:outline-none focus:ring-pink-200 dark:focus:ring-pink-800 font-medium rounded-lg text-sm px-5 py-2.5 text-center transition duration-300 ease-in-out' %>
    <% else %>
        <%= link_to 'Login', new_user_session_path, class: 'text-white bg-gradient-to-br from-blue-500 to-cyan-500 hover:from-blue-600 hover:to-cyan-600 focus:ring-4 focus:outline-none focus:ring-blue-200 dark:focus:ring-blue-800 font-medium rounded-lg text-sm px-5 py-2.5 text-center transition duration-300 ease-in-out' %>

        <%= link_to 'Sign Up', new_user_registration_path, class: 'text-white bg-gradient-to-br from-green-400 to-lime-500 hover:from-green-500 hover:to-lime-600 focus:ring-4 focus:outline-none focus:ring-green-200 dark:focus:ring-green-800 font-medium rounded-lg text-sm px-5 py-2.5 text-center transition duration-300 ease-in-out' %>
    <% end %>
    </div>
  </div>
</nav>

<!-- Main Content Area -->
<main class="container mx-auto p-6 flex-grow">
  <% if notice %>
    <div class="flex items-center bg-green-100 border border-green-200 text-green-700 px-4 py-3 rounded-md shadow-sm mb-4" role="alert">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
      </svg>
      <span><%= notice %></span>
    </div>
  <% end %>
  <% if alert %>
    <div class="flex items-center bg-red-100 border border-red-200 text-red-700 px-4 py-3 rounded-md shadow-sm mb-4" role="alert">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
      <span><%= alert %></span>
    </div>
  <% end %>

  <!-- Yield Content -->
  <%= yield %>
</main>

<!-- Footer -->
<footer class="bg-white shadow-inner py-4 text-center text-gray-600">
  &copy; <%= Time.current.year %> ProjectConversation. All rights reserved.
</footer>
</body>
</html>
